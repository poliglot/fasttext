% TODO Pass useTitleProgressBar and nooffset instead of modifying sty file

\urlstyle{same}
\usepackage[overlay,absolute]{textpos}

% Beamer's left and right margin is 10 mm. The slide dimensions are
% 128 mm x 96 mm. The resulting \TPHorizModule = 12 mm and
% \TPVertModule = 10 mm.
%\TPGrid[10 mm,8 mm]{9}{8}

%\usepackage{unicode-math}

\usepackage{tikz}
\usepackage{tikz-dependency}
